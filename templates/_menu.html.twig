<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('default') }}">Boutique</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="/">Accueil<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item dropdown active">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Produits
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="/produits/marque/Apple">Apple</a>
                    <a class="dropdown-item" href="/produits/marque/Microsoft">Microsoft</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('produits') }}">Tous les produits</a>
                </div>
            </li>
            {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item dropdown active">
                    <a class="nav-link dropdown-toggle" href="{{ path('admin_produit') }}" id="navbarDropdownAdmin" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Admin
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdownAdmin">
                        <a class="dropdown-item" href="{{ path('admin_produit') }}">Produits</a>
                        <a class="dropdown-item" href="{{ path('marque_index') }}">Marques</a>
                        <a class="dropdown-item" href="{{ path('admin_produits_stock') }}">Gestion des stocks</a>
                    </div>
                </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav my-2 my-lg-0">
            {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                <p class="mb-0 my-2 text-white mr-3">{{ app.user.email }}</p>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>