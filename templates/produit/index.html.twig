{% extends 'base.html.twig' %}

{% block title %}Nos produits en stock{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="display-4 mt-3"> Nos produits en stock</h2>
        <form class="pull-right hidden" role="search" id="nav-search-form" method="get" action="{{ path('produits_search') }}">
            <div class="row m-4">
                <div class="col-10">
                    <input name="search" type="text" class="form-control" id="search"  placeholder="Titre ou description" />
                </div>
                <div class="col">
                    <button type="submit" class="btn btn-outline-primary">Rechercher</button>
                </div>

            </div>
        </form>

        </a>
        <div class="row flex">
            {% for produit in produits %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('produits_detail',{slug:produit.slug}) }}">{{ produit.titre }}</a>
                            </h5>
                            <p class="card-text">{{ produit.description }}</p>
                            <p class="card-text">{{ produit.poids|number_format(1, ',', '.') }} Kgs</p>
                            <div class="text-primary" style="font-weight: bold;font-size: 2rem">{{ produit.prixTTC | number_format(0,'',' ')}} € TTC</div>
                        </div>
                    </div>
                </div>
            {% endfor %}
            <div class="pagination-nav">
            {{ knp_pagination_render(produits) }}
            </div>
        </div>
    </div>
{% endblock %}
